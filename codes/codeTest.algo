ALGORITHME NomAlgo
constante:
	REMILAURET <- "ppMax"
variable:
	x, y	: entier
	a:booleen
	b:entier
	c:reel
	d:caractere
	christophe:chaine	
DEBUT

ecrire(estEntier("3"))
ecrire ( car(1))
ecrire ( car(ord('@')) )
	


	

FIN