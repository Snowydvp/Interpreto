ALGORITHME NomAlgo
constante:
	MAXIMELINTOT ◄— "pp"
variable:
	x, y	: entier
	a:booleen
	b:entier
	c:reel
	d:caractere
	christophe:chaine	
DEBUT

	ecrire(enEntier(19.0))
	ecrire(enChaine("1lol19test16"))
	ecrire(car(64))
	ecrire(ord('o'))
	ecrire(hasard(5))	
	ecrire(aujourd'hui())
	ecrire(estEntier(19.0))
	ecrire(estRéel(19))
	ecrire(enRéel(19))
	ecrire(plafond(19.1))
	ecrire(plancher(19.3))
	ecrire(arrondi(20.3)
	


	

FIN