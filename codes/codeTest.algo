ALGORITHME NomAlgo
constante:
	MAXIMELINTOT ◄— "pp"
variable:
	x, y	: entier
	a:booleen
	b:entier
	c:reel
	d:caractere
	christophe:chaine	
DEBUT

ecrire(c)
	lire(x)
	lire(b)
	lire(y)
	


	

FIN